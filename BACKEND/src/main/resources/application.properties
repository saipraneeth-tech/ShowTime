# ===============================
# DATABASE CONFIG
# ===============================
# Local Database (Commented out)
# spring.datasource.url=${SHOWTIME_DB_URL:jdbc:mysql://localhost:3306/showtime?createDatabaseIfNotExist=true}
# spring.datasource.username=${SHOWTIME_DB_USERNAME:root}
# spring.datasource.password=${SHOWTIME_DB_PASSWORD:Thilak@123}

# Railway Database
# WARNING: Hardcoding credentials is not safe for public repositories. 
# Use Environment Variables (RAILWAY_DB_URL, etc.) in production/deployment.
# The defaults below are for your current local testing based on the screenshot.
# Railway Database
# WARNING: Hardcoding credentials is not safe for public repositories. 
# Use Environment Variables (RAILWAY_DB_URL, etc.) in production/deployment.
# The defaults below are for your current local testing based on the screenshot.
spring.datasource.url=${RAILWAY_DB_URL:jdbc:mysql://localhost:3306/showtime?createDatabaseIfNotExist=true}
spring.datasource.username=${RAILWAY_DB_USERNAME:root}
spring.datasource.password=${RAILWAY_DB_PASSWORD}

# ===============================
# JPA / HIBERNATE CONFIG
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# SERVER
# ===============================
server.port=8080

# ===============================
# EMAIL CONFIG (Gmail SMTP)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# spring.mail.username=YOUR_GMAIL
# spring.mail.password=YOUR_APP_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Twilio SMS Configuration
twilio.account.sid=${TWILIO_ACCOUNT_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.verify.service.sid=${TWILIO_VERIFY_SERVICE_SID}
twilio.phone.number=${TWILIO_PHONE_NUMBER}
